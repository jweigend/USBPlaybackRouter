#!/usr/bin/env python3
"""Launcher: run the package from the repository checkout without installing."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from usb_playback_router.cli import main  # noqa: E402

sys.exit(main())
